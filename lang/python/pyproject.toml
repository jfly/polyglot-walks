[project]
name = "python"
version = "0.1.0"
authors = [
    { name = "Jeremy Fleischman", email = "jeremyfleischman@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
walk-python = "python:walk"

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"
